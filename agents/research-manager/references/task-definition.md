# Task Definition

Before any run begins, ResearchManager should make the following explicit:

- benchmark title and DOI
- why this benchmark is being reconstructed
- what counts as a row in the final dataset
- what must never be extracted
- what evidence each row must preserve
- what qualifies a run for release
