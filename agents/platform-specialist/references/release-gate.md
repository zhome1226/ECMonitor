# Release Gate

PlatformSpecialist may publish only when:

- validated outputs are explicitly approved
- analytics outputs cite validated inputs only
- release artifacts are versioned
- unresolved or excluded items are documented
